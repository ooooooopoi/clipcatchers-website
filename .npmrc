# next-auth's beta declares an optional peer on nodemailer <=8, but 9.x is
# required to clear published advisories. The dependency is only used by
# next-auth's email provider, which this app doesn't use — it calls nodemailer
# directly — so the peer range is safe to override.
legacy-peer-deps=true
