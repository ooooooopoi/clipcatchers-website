{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'echo \"[boot] PORT=${PORT:-unset} -> binding 3000\"; ( npx prisma migrate deploy && echo \"[migrate] applied\" || echo \"[migrate] FAILED - check DATABASE_URL\" ) & exec npx next start -H 0.0.0.0 -p 3000'",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
